<h2>Администрирование</h2>
<br>
<p>Справочники:</p>
<p><%= link_to("Приложения", apps_path, class: "btn btn-primary btn-lg enabled") if user_signed_in? %></p>
<p><%= link_to("Роли", roles_path, class: "btn btn-primary btn-lg enabled") if user_signed_in? %></p>
<p><%= link_to("Пользователи", users_path, class: "btn btn-primary btn-lg enabled") if user_signed_in? %></p>
<p><%= link_to("Салоны", salons_path, class: "btn btn-primary btn-lg enabled") if user_signed_in? %></p>
